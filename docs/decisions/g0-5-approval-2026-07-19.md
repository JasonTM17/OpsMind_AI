# G0.5 Full Baseline Approval Record

## Decision

The project owner approved the complete concrete baseline in
[g0-5-recommended-baseline.md](./g0-5-recommended-baseline.md) for the G0.5
product and production contract.

Canonical evidence URI:
`opsmind://governance/g0-5/2026-07-19/full-baseline-approval`

## Approval Metadata

| Field | Value |
|---|---|
| Approver identity | `project-owner-user` |
| Accountable role | Project owner / product sponsor |
| Approved at (UTC) | `2026-07-19T16:03:27.175Z` |
| Codex task | `019f79ea-08ed-75c1-a1c3-adbf992abda4` |
| Scope | All twelve G0.5 baseline decisions |

## Source Statement

The approving user stated in the active Codex task:

> Tiếp tục làm đi bạn tôi phê duyệt toàn bộ quyền cho bạn

For governance purposes, this statement records approval of the complete G0.5
recommended baseline that was awaiting the user's decision. It does not grant
standing permission for destructive filesystem operations, secret disclosure,
production deployment, external publication, git commit, or git push. Those
operations retain their own task-scoped authorization and safety gates.

## Approved Decisions

The authoritative typed values, constraints, owner roles, timestamps, and this
evidence URI are recorded in
[product-production-contract.json](./product-production-contract.json). The
approved keys are:

1. `deploymentArchetype`
2. `targetEnvironment`
3. `tenantModel`
4. `identityProfile`
5. `deepseekEgressPolicy`
6. `firstLiveIntegration`
7. `evidenceArtifactStore`
8. `loadEnvelope`
9. `serviceObjectives`
10. `dataLifecycle`
11. `operationalOwnership`
12. `deliveryCapacity`

## Consequences

- Phase 1 may complete only after the strict validator returns `PASS` with exit
  code `0`.
- The DeepSeek policy permits only allowlisted and redacted data classes. It
  does not approve storing the provider API key in the repository or chat-derived
  artifacts.
- Managed Kubernetes in Singapore is the production target; local and CI
  implementation remains container-based until the delivery phases.
- Prometheus against synthetic non-production metrics is the only approved
  first live connector, and it is read-only.
- These initial load and service objectives must be revalidated with measured
  G3 evidence before scale or production promotion decisions.
